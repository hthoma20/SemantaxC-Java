main module Main {
    @bind(1, int);
    @bind(2, string);
    @bind(3, type);
    @bind(4, bool);
    @bind(5, array(int));
    @bind(6, array(string));
    @bind(7, array(array(int)));
    @bind(8, record());
    @bind(9, record(x: int));
    @bind(10, record(x: array(int)));
    @bind(11, record(
        x: int,
        y: record(
            a: string,
            b: array(bool)
        )
    ));
}
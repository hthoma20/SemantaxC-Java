main module multiplePatterns {

    $a + b$
    (a: int, b: int) -> int {
        @addint(a, b)
    }

    $a < b$
    (a: int, b: int) -> bool {
        false
    }

    $a and b$
    (a: bool, b: bool) -> bool {
        @return(false);
    }

    5 < 6 and 10 < 4;

}
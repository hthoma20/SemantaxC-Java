main module Main {
    @bind(0, int);
    @bind(1, string);
    @bind(2, type);
    @bind(3, bool);

    @bind(4, array(int));
    @bind(5, array(string));
    @bind(6, array(array(int)));

    @bind(7, record());
    @bind(8, record(x: int));
    @bind(9, record(x: array(int)));
    @bind(10, record(
        x: int,
        y: record(
            a: string,
            b: array(bool)
        )
    ));

    @bind(11, func{});
    @bind(12, func{->});
    @bind(13, func{string});
    @bind(14, func{string->});
    @bind(15, func{->int});
    @bind(16, func{string->int});
}